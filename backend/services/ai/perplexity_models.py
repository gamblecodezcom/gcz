PERPLEXITY_MODELS = {
    "fast": "sonar",          # lightweight, cheap, quick responses
    "pro": "sonar-pro",       # strongest reasoning + search
    "embed": "sonar-embed",   # embeddings endpoint
    "search": "sonar-pro"     # search uses the pro model
}
